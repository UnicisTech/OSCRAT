{
  "phases": {
    "setup": {
      "nixPkgs": ["...", "nodejs", "syft", "grype", "python3", "python3Packages.pipx"]
    },
    "openscap": {
      "dependsOn": ["install"],
      "cmds": ["PIPX_BIN_DIR=/usr/local/bin pipx install openscap-report==1.0.0"]
    }
  }
}
