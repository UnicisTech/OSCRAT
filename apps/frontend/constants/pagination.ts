/**
 * Single source of truth for listing page size. Every paginated listing in the
 * app reuses this so users see a consistent number of rows across SBOM,
 * vulnerabilities, audit logs, products, versions, tasks, etc.
 */
export const LISTING_PAGE_SIZE = 15;
