{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm install --frozen-lockfile",
    "watchPatterns": ["railway_script_runner.json"]
  },
  "deploy": {
    "startCommand": "pnpm db:generate && ${SCRIPT_COMMAND:-echo 'No script specified. Set SCRIPT_COMMAND variable to run a script.'}",
    "restartPolicyType": "never"
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production",
        "DATABASE_URL": { "$ref": "DATABASE_URL" },
        "SCRIPT_COMMAND": "echo 'Set SCRIPT_COMMAND to your desired script (e.g., pnpm seed)'"
      }
    }
  }
}
