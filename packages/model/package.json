{
  "name": "@oscrat/model",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "studio": "prisma studio",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "reset": "prisma migrate reset",
    "reset:force": "prisma migrate reset --force",
    "seed:oscrat": "npx tsx prisma/seed-oscrat.ts",
    "build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0",
    "tsx": "^4.20.3"
  }
}
