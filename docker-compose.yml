version: '3'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: counter
      POSTGRES_USER: postgres
      POSTGRES_DB: unicis_platform
    ports:
      - 5433:5432
